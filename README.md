# AI Ассистент Маркетолога (Enhanced Version)

Улучшенная версия системы транскрибации и анализа звонков с помощью искусственного интеллекта.

## 🆕 Новые возможности в Enhanced версии v3.0

### Полноценная диаризация (NEW!)
- ⭐ **Автоматическое определение говорящих**: Система сама определяет сколько человек участвует в разговоре
- ⭐ **Разделение реплик по спикерам**: Каждая фраза привязана к конкретному говорящему
- ⭐ **Автоматическое присвоение ролей**: Qwen анализирует содержание и определяет кто Клиент, кто Сотрудник
- ⭐ **Поддержка множественных участников**: Работает с 2, 3, 4+ говорящими в одном звонке

### Просмотр транскриптов
- **Просмотр транскриптов**: Полные транскрипты звонков прямо на странице результатов исследования
- **Временные метки**: Каждый сегмент транскрипта отображается с временными метками начала и конца
- **Удобный интерфейс**: Модальное окно с timeline для комфортного изучения транскрипции
- **Сравнение с результатами**: Возможность проверить, как LLM-анализ соответствует реальному содержанию звонка

## Основные функции

1. **Транскрибация звонков**
   - Использует локальную модель Whisper от OpenAI для преобразования аудио (MP3/WAV) в текст
   - Поддерживает GPU-ускорение через CUDA
   - Сохраняет транскрипты в JSON-формате с временными метками

2. **Диаризация (определение говорящих) 🆕**
   - Автоматически определяет количество говорящих в разговоре
   - Разделяет реплики между SPEAKER_00, SPEAKER_01, и т.д.
   - Использует pyannote.audio 3.1 (state-of-the-art модель)
   - Работает на CPU и GPU

3. **Автоматическое определение ролей 🆕**
   - Qwen анализирует содержание реплик каждого спикера
   - Автоматически определяет кто Клиент, кто Сотрудник
   - Работает с любым количеством участников
   - Fallback на эвристику если LLM недоступен

4. **Анализ с помощью LLM**
   - Использует локальную модель Qwen3:8b через Ollama
   - Анализирует транскрипты по произвольным запросам пользователя
   - Извлекает ключевые находки, цитаты, инсайты из разговоров
   - Учитывает роли говорящих при анализе

5. **Просмотр транскриптов**
   - Кнопка "Транскрипт" на странице результатов для каждого звонка
   - Полный текст разговора в одном блоке
   - Детальная транскрипция с временными метками для каждого сегмента
   - Информация о говорящих и их ролях
   - Timeline с цветовым кодированием сегментов

6. **Веб-интерфейс (React + TypeScript)**
   - Страница "Звонки" - загрузка и управление аудиофайлами
   - Страница "Новое исследование" - создание аналитических запросов
   - Страница "Результаты" - просмотр результатов анализа и транскриптов

## 🚀 Быстрый запуск (3 способа)

### ⭐ Способ 1: Самый простой (Рекомендуется)

Просто дважды кликните на файл:
```
start.bat
```

Всё запустится автоматически:
- ✅ Создадутся нужные папки
- ✅ Запустятся Backend и Frontend
- ✅ Откроется браузер
- ✅ Готово к работе!

### Способ 2: Минималистичный

```
start_simple.bat
```

Серверы запустятся в минимизированных окнах.

### Способ 3: Ручной

**Терминал 1 (Backend):**
```bash
cd project-036-enhanced
start_backend.bat
```

**Терминал 2 (Frontend):**
```bash
cd project-036-enhanced
start_frontend.bat
```

Откройте: `http://localhost:5173`

---

## Установка (только при первом запуске)

### Предварительные требования

1. **Python 3.11+** установлен
2. **Node.js 18+** установлен
3. **Ollama** установлен и запущен
4. **Модель Qwen3:8b** загружена
5. **HuggingFace токен** (для диаризации) - см. [НАСТРОЙКА_ДИАРИЗАЦИИ.md](НАСТРОЙКА_ДИАРИЗАЦИИ.md)

### Установка Ollama и модели

```bash
# 1. Скачайте Ollama с https://ollama.com
# 2. Загрузите модель:
ollama pull qwen3:8b
```

### Установка зависимостей (автоматически при первом запуске)

Если запускаете `start.bat` - зависимости установятся автоматически!

Или вручную:

```bash
# Backend
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# Frontend
cd frontend
npm install
cd ..
```

## Как использовать новую функцию просмотра транскриптов

1. Загрузите MP3/WAV файлы звонков через страницу "Звонки"
2. Создайте новое исследование на странице "Новое исследование"
3. Выберите звонки и введите исследовательский запрос
4. Дождитесь завершения обработки
5. На странице "Результаты" нажмите кнопку "Транскрипт" рядом с любым звонком
6. В открывшемся окне вы увидите:
   - Полный текст разговора вверху
   - Детальную транскрипцию с временными метками внизу
   - Каждый сегмент показывает время начала и окончания фразы

## Архитектура

- **Frontend**: React 18, Ant Design, Vite (порт 5173)
- **Backend**: FastAPI, SQLAlchemy (порт 8000)
- **ML Pipeline**: Whisper (локально) + Ollama (Qwen3:8b)
- **Хранение**: SQLite БД + файловая система для аудио и транскриптов

## API Endpoints

### Новый endpoint:
- `GET /api/calls/{call_id}/transcript` - получение транскрипта звонка

### Существующие endpoints:
- `POST /api/calls/upload` - загрузка аудиофайлов
- `GET /api/calls` - список звонков
- `DELETE /api/calls/{id}` - удаление звонка
- `POST /api/analysis` - создание исследования
- `GET /api/analysis/{id}` - статус исследования
- `GET /api/analysis/{id}/results` - результаты исследования

## Структура проекта

```
project-036-enhanced/
├── backend/
│   └── app/
│       ├── routes/
│       │   ├── calls.py          # ✨ Обновлено: добавлен endpoint транскрипта
│       │   └── analysis.py
│       ├── services/
│       │   ├── pipeline.py
│       │   └── ollama_client.py
│       ├── main.py
│       ├── models.py
│       └── config.py
├── frontend/
│   └── src/
│       ├── pages/
│       │   ├── AnalysisResultsPage.tsx  # ✨ Обновлено: добавлен просмотр транскриптов
│       │   ├── NewAnalysisPage.tsx
│       │   └── CallsPage.tsx
│       ├── api/
│       │   └── client.ts         # ✨ Обновлено: добавлен метод getCallTranscript
│       └── App.tsx
├── data/                          # Создается автоматически
│   ├── audio/                     # Загруженные аудиофайлы
│   ├── transcripts/               # JSON транскрипты
│   └── results/                   # Результаты анализов
├── requirements.txt
├── SETUP.md
└── README.md
```

## Отличия от базовой версии

| Функция | Базовая версия | Enhanced версия |
|---------|----------------|-----------------|
| Просмотр транскриптов | ❌ Нет | ✅ Есть |
| Временные метки | Только в JSON | ✅ В UI с форматированием |
| Сравнение с анализом | ❌ Невозможно | ✅ Доступно на одной странице |
| API для транскриптов | ❌ Нет | ✅ GET /calls/{id}/transcript |
| UI компоненты | Базовые таблицы | ✅ + Modal + Timeline |

## Требования

- Python 3.11+
- Node.js 18+
- Ollama с моделью Qwen3:8b
- (Опционально) CUDA для GPU-ускорения Whisper

## Стоимость

- Whisper API: Не требуется (используется локально)
- Ollama: Бесплатно (работает локально)
- Полностью бесплатное решение без облачных сервисов

## Устранение неполадок

См. файл TROUBLESHOOTING.md в оригинальном проекте.

### Новые возможные проблемы:

**Ошибка "Транскрипт недоступен"**
- Убедитесь, что звонок был обработан (статус "processed")
- Проверьте наличие файла в `data/transcripts/{call_id}.json`

**Модальное окно не открывается**
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что backend запущен и доступен

## Лицензия

Этот проект создан для образовательных целей. Используйте на свой страх и риск.

## Changelog

### v2.0 (Enhanced) - 2026-01-16
- ✨ Добавлен просмотр транскриптов на странице результатов
- ✨ Добавлены временные метки в удобном формате
- ✨ Новый API endpoint для получения транскриптов
- 🎨 Улучшен UI с использованием Modal и Timeline компонентов
- 📝 Обновлена документация

### v1.0 (Base)
- Базовая функциональность транскрибации и анализа
