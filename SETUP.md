# Инструкция по запуску AI Ассистента Маркетолога (MVP)

## Предварительные требования

1. **Python 3.11+** с установленным pip
2. **Node.js 18+** с npm
3. **CUDA** (для GPU-ускорения Whisper, опционально)
4. **Ollama** установлен и запущен локально
5. **Qwen3:8b модель** загружена в Ollama

## Установка Ollama и модели

1. Скачайте и установите Ollama с https://ollama.com
2. Загрузите модель:
```bash
ollama pull qwen3:8b
```
3. Проверьте, что Ollama работает:
```bash
ollama run qwen3:8b
```

## Установка Backend

1. Создайте виртуальное окружение:
```bash
python -m venv .venv
```

2. Активируйте окружение:
   - Windows: `.\.venv\Scripts\activate`
   - Linux/Mac: `source .venv/bin/activate`

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Убедитесь, что Whisper установлен. Если нет, установите:
```bash
pip install openai-whisper
```

Для GPU-ускорения установите PyTorch с CUDA:
```bash
# Проверьте актуальную команду на https://pytorch.org
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

## Установка Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Вернитесь в корень проекта:
```bash
cd ..
```

## Запуск приложения

### 🚀 Быстрый способ (рекомендуется)

Просто запустите файл **`start.bat`** — он автоматически запустит оба сервера!

Или по отдельности:
- **`start_backend.bat`** — запускает только Backend
- **`start_frontend.bat`** — запускает только Frontend

После запуска откройте браузер: **http://localhost:5173**

---

### Ручной запуск (если нужно)

#### 1. Запуск Backend

В корне проекта (с активированным `.venv`):
```bash
uvicorn backend.app.main:app --reload --port 8000
```

Backend будет доступен на `http://localhost:8000`

#### 2. Запуск Frontend

В другом терминале, в директории `frontend`:
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## Структура проекта

```
project-036/
├── backend/              # Python backend
│   └── app/
│       ├── main.py      # Точка входа FastAPI
│       ├── config.py    # Настройки
│       ├── db.py        # База данных SQLite
│       ├── models.py    # ORM модели
│       ├── schemas.py   # Pydantic схемы
│       ├── routes/      # API эндпоинты
│       └── services/    # ML пайплайн
├── frontend/            # React frontend
│   └── src/
│       ├── pages/       # Страницы приложения
│       └── api/         # API клиент
├── data/                # Данные (создаётся автоматически)
│   ├── audio/          # Загруженные аудиофайлы
│   ├── transcripts/    # Транскрипты
│   └── results/        # Результаты анализов
└── requirements.txt     # Python зависимости
```

## Первое использование

1. Откройте браузер на `http://localhost:5173`
2. Перейдите на страницу "Звонки"
3. Загрузите один или несколько MP3/WAV файлов
4. Перейдите на "Новое исследование"
5. Выберите звонки и введите исследовательский запрос
6. Дождитесь завершения обработки
7. Просмотрите результаты на странице "Результаты"

## Возможные проблемы

### Ollama не отвечает
- Убедитесь, что Ollama запущен: `ollama serve`
- Проверьте порт: `http://localhost:11434`

### Whisper не работает
- Проверьте установку: `pip list | grep whisper`
- Для GPU убедитесь, что CUDA установлена: `python -c "import torch; print(torch.cuda.is_available())"`

### Ошибки при загрузке файлов
- Проверьте формат файлов (только MP3 и WAV)
- Убедитесь, что директория `data/audio` создана

### База данных
- База данных SQLite создаётся автоматически в `data/app.db`
- При необходимости удалите файл для сброса данных

## Конфигурация

Настройки находятся в `backend/app/config.py`. Можно переопределить через переменные окружения:

- `DATA_ROOT` - корневая директория данных
- `OLLAMA_BASE_URL` - URL Ollama (по умолчанию http://localhost:11434)
- `OLLAMA_MODEL_NAME` - название модели (по умолчанию qwen3:8b)
- `WHISPER_MODEL_NAME` - модель Whisper (по умолчанию large-v3)
- `MAX_CONCURRENT_TRANSCRIPTIONS` - параллелизм транскрибации (по умолчанию 4)


