# 🚀 Быстрый старт - Транскрибация звонков

## ⚡ За 5 минут к результату

### 1. Установите API ключ OpenAI
```powershell
$env:OPENAI_API_KEY="sk-your-api-key-here"
```

### 2. Поместите MP3 файлы в папку `calls`
- Имя файла = имя менеджера (например: `Иван_Петров.mp3`)
- Поддерживается только формат MP3

### 3. Запустите транскрибацию
```bash
python advanced_transcriber.py
```

### 4. Получите результат
Excel файл `transcribed_calls_advanced.xlsx` с тремя листами:
- **Все реплики** - основная таблица
- **Сводка по менеджерам** - статистика
- **Статистика ролей** - распределение

## 📋 Структура Excel файла

| Имя менеджера | Роль | Реплика | Время начала | Время окончания |
|---------------|------|---------|--------------|-----------------|
| Иван_Петров   | Менеджер | Здравствуйте, чем могу помочь? | 0.0 | 2.5 |
| Иван_Петров   | Клиент | Хочу заказать услугу | 3.0 | 5.2 |

## 🔧 Настройка ролей

Отредактируйте файл `advanced_transcriber.py`:

```python
self.manager_keywords = [
    'здравствуйте', 'добрый день', 'чем могу помочь',
    # Добавьте свои фразы
]

self.client_keywords = [
    'хочу заказать', 'сколько стоит', 'интересует',
    # Добавьте свои фразы
]
```

## 💰 Стоимость
- **Whisper API**: $0.006 за минуту
- **10 звонков по 5 минут**: ~$0.30

## ❓ Проблемы?

1. **API ключ не найден**: Установите переменную окружения
2. **MP3 не найдены**: Создайте папку `calls` и поместите файлы
3. **Ошибка транскрибации**: Проверьте API ключ и кредиты OpenAI

## 📞 Поддержка
Запустите тест: `python test_setup.py` 