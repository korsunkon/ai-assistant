# Настройка диаризации (определение говорящих)

## Что такое диаризация?

Диаризация - это процесс автоматического определения "кто и когда говорил" в аудиозаписи.

В новой версии приложение:
1. **Определяет количество говорящих** (автоматически)
2. **Разделяет реплики по спикерам** (SPEAKER_00, SPEAKER_01, ...)
3. **Присваивает роли** через анализ Qwen (Клиент, Сотрудник, etc.)

## Требования

Для работы диаризации нужна **модель pyannote** (бесплатно).

Есть два способа настройки:

### 🎯 СПОСОБ 1: Локальная модель (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Полностью локально, без интернета
- ✅ Не нужен HuggingFace токен при каждом запуске
- ✅ Быстрее загружается

**Шаги:**

#### Шаг 1: Установите git-lfs

Git LFS нужен для скачивания больших файлов моделей.

**Windows:**
1. Скачайте: https://git-lfs.github.com/
2. Установите и запустите: `git lfs install`

**Linux:**
```bash
sudo apt-get install git-lfs
git lfs install
```

#### Шаг 2: Скачайте модель локально

```bash
cd project-036-enhanced
git clone https://huggingface.co/pyannote/speaker-diarization-3.1
```

**Важно:** Для скачивания с HuggingFace нужен токен (только один раз!):
1. Зарегистрируйтесь: https://huggingface.co/join
2. Примите условия: https://huggingface.co/pyannote/speaker-diarization-3.1
3. Создайте токен: https://huggingface.co/settings/tokens
4. При запросе git введите:
   - Username: `your_hf_username`
   - Password: `your_hf_token`

#### Шаг 3: Настройте путь в config.py

В файле [backend/app/config.py](project-036-enhanced/backend/app/config.py):

```python
# Путь к локальной модели (относительно корня проекта)
diarization_model: str = "./speaker-diarization-3.1"
```

#### Шаг 4: Проверка

Запустите приложение. В логах Backend должно быть:

```
INFO: Загружаю модель диаризации: ./speaker-diarization-3.1
INFO: Загрузка локальной модели из: C:\...\speaker-diarization-3.1
INFO: Диаризация будет выполняться на GPU (или CPU)
```

✅ **Готово! Теперь всё работает локально без токенов.**

---

### 🌐 СПОСОБ 2: Загрузка с HuggingFace

Если не хотите скачивать модель заранее, можно загружать с HuggingFace при каждом запуске (требует интернет и токен).

#### Шаг 1: Создайте HuggingFace токен

1. Зарегистрируйтесь: https://huggingface.co/join
2. Создайте токен: https://huggingface.co/settings/tokens (тип: Read)
3. Примите условия: https://huggingface.co/pyannote/speaker-diarization-3.1

#### Шаг 2: Установите токен

**Windows (Command Prompt):**
```cmd
set HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxx
```

**Или создайте файл .env:**
```bash
# project-036-enhanced/.env
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxx
```

#### Шаг 3: Настройте config.py

```python
# Загрузка с HuggingFace
diarization_model: str = "pyannote/speaker-diarization-3.1"
```

#### Шаг 4: Проверка

Запустите приложение и загрузите звонок. В логах Backend должно быть:

```
INFO: Загружаю модель диаризации: pyannote/speaker-diarization-3.1
INFO: Загрузка модели с HuggingFace (требуется токен)
INFO: Диаризация будет выполняться на GPU (или CPU)
```

---

## Какой способ выбрать?

| Критерий | Способ 1: Локально | Способ 2: HuggingFace |
|----------|-------------------|----------------------|
| Интернет при работе | ❌ Не нужен | ✅ Нужен |
| HuggingFace токен | Только для скачивания | При каждом запуске |
| Скорость загрузки | ⚡ Быстро | 🐌 Медленнее |
| Размер на диске | ~500 МБ | ~50 МБ (кэш) |
| **Рекомендация** | ✅ **Для продакшена** | Для тестирования |

**Вывод:** Используйте **Способ 1 (локальная модель)** для полностью автономной работы.

## Настройка параметров

Параметры диаризации в [backend/app/config.py](project-036-enhanced/backend/app/config.py):

```python
# Диаризация
diarization_enabled: bool = True  # Включить/выключить
min_speakers: int | None = None   # Мин. говорящих (None = авто)
max_speakers: int | None = None   # Макс. говорящих (None = авто)

# Определение ролей
role_assignment_enabled: bool = True  # Через Qwen
```

### Примеры настройки:

**Если всегда 2 говорящих (клиент + сотрудник):**
```python
min_speakers = 2
max_speakers = 2
```

**Если может быть от 2 до 4:**
```python
min_speakers = 2
max_speakers = 4
```

**Автоопределение (по умолчанию):**
```python
min_speakers = None
max_speakers = None
```

### Отключение диаризации:

Если не хотите использовать диаризацию (например, нет GPU):

```python
diarization_enabled = False
```

Приложение будет работать в fallback режиме (как раньше).

### Отключение определения ролей:

Если хотите только диаризацию, но без определения ролей через Qwen:

```python
role_assignment_enabled = False
```

Роли будут просто "SPEAKER_00", "SPEAKER_01", etc.

## Требования к ресурсам

### CPU режим:
- ✅ Работает на любом ПК
- ⚠️ Медленно (1-2 минуты на звонок)
- Требует: ~4 ГБ RAM

### GPU режим (рекомендуется):
- ✅ Быстро (10-20 секунд на звонок)
- ✅ Более точно
- Требует:
  - NVIDIA GPU с CUDA
  - ~2 ГБ VRAM
  - PyTorch с CUDA support

### Установка PyTorch с CUDA:

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

Проверка GPU:
```python
import torch
print(torch.cuda.is_available())  # Должно быть True
```

## Возможные проблемы

### Ошибка: "HuggingFace token required"

**Решение для Способа 1 (локально):**
- Убедитесь что папка `speaker-diarization-3.1` существует
- Проверьте путь в config.py: `diarization_model = "./speaker-diarization-3.1"`

**Решение для Способа 2 (HuggingFace):**
- Установите токен: `set HF_TOKEN=your_token`

### Ошибка: "You must agree to the model's license"

**Решение:**
- Откройте: https://huggingface.co/pyannote/speaker-diarization-3.1
- Нажмите "Agree and access repository"

### Ошибка: "CUDA out of memory"

**Решение:**
- Закройте другие программы
- Уменьшите `max_concurrent_transcriptions` в config.py
- Используйте CPU режим

### Диаризация работает, но медленно

**Решение:**
- Установите PyTorch с CUDA (см. выше)
- Проверьте что GPU используется в логах

### Роли определяются неправильно

**Возможные причины:**
- Звонок очень короткий (мало данных для анализа)
- Нестандартный диалог
- Qwen не запущен

**Решение:**
- Проверьте что Ollama запущен: `ollama run qwen3:8b`
- Посмотрите логи Backend для подробностей
- Можно отключить: `role_assignment_enabled = False`

## Как это выглядит

### До (старая версия):
```json
{
  "segments": [{
    "text": "весь разговор одним куском",
    "speaker": "Сотрудник"
  }]
}
```

### После (с диаризацией):
```json
{
  "segments": [
    {
      "start": 0.0,
      "end": 2.5,
      "text": "Здравствуйте, компания X",
      "speaker": "SPEAKER_00",
      "role": "Сотрудник"
    },
    {
      "start": 2.5,
      "end": 5.0,
      "text": "Добрый день, хочу заказать",
      "speaker": "SPEAKER_01",
      "role": "Клиент"
    }
  ],
  "num_speakers": 2,
  "speaker_roles": {
    "SPEAKER_00": "Сотрудник",
    "SPEAKER_01": "Клиент"
  }
}
```

## Дополнительные ресурсы

- pyannote.audio документация: https://github.com/pyannote/pyannote-audio
- HuggingFace токены: https://huggingface.co/docs/hub/security-tokens
- Модель диаризации: https://huggingface.co/pyannote/speaker-diarization-3.1

---

**Версия:** 3.0 (с полноценной диаризацией)
**Дата:** 2026-01-19

**Sources:**
- [GitHub - BUTSpeechFIT/DiariZen](https://github.com/BUTSpeechFIT/DiariZen)
- [Speaker Diarization in Python](https://dev.to/gracezzhang/speaker-diarization-in-python-235i)
- [How to perform Speaker Diarization in Python](https://www.assemblyai.com/blog/speaker-diarization-python)
